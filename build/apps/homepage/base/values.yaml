enableRbac: true
serviceAccount:
  create: true
config:
  bookmarks:
  services:
  widgets:
    - kubernetes:
        cluster:
          show: true
          cpu: true
          memory: true
    - widget:
        type: pihole
        url: http://pihole-dashboard.pihole.svc
    - longhorn:
        expanded: true
        total: true
        labels: true
        nodes: true
  kubernetes:
    mode: cluster
  settings:
    providers:
      longhorn:
        url: http://longhorn-frontend.longhorn-system.svc