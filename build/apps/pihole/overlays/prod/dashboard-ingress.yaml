apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pihole-dashboard
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`dns.graytonward.com`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: pihole-dashboard
          port: dashboard