apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pihole-dashboard
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`dns.{{ traefik_ssl.base_domain }}`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: pihole-dashboard
          port: dashboard